{
  "name": "@apibase/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "author": "Jason Schilling <jason.schilling@sourecode.de>",
  "license": "MIT",
  "scripts": {
    "clean:dist": "rm -rf dist",
    "clean:docs": "rm -rf docs",
    "clean:git": "git clean -dXf",
    "clean": "npm run clean:dist && npm run clean:docs && npm run clean:git",
    "test": "jest",
    "build": "tsc",
    "build-docs": "typedoc --out docs --theme markdown",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@apibase/core": "^1.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.4",
    "@types/express": "^4.16.0",
    "@types/jest": "^23.3.2",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.1",
    "typescript": "^3.0.1",
    "typedoc": "^0.12.0",
    "typedoc-plugin-markdown": "^1.1.15"
  }
}
